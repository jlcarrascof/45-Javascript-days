function example() {
    var number = 42; // Variable declared in the function scope
    console.log(number); // What is the value of 'numero' here?
}
example();
console.log(number); // What happens if we try to access 'numero' here?
